<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('events', function (Blueprint $table) {
            $table->id();
            $table->string('title');
            $table->text('description');
            $table->date('date');
            $table->string('location');
            $table->integer('available_seats');
            $table->enum('status', ['Available', 'Not available']);
            $table->enum('validation', ['Awaiting validation', 'Validated', 'Unvalidated'])->defaultValue('Awaiting validation');
            $table->enum('acceptance', ['Automatic acceptance', 'Manual acceptance']);
            $table->foreignId('organiser_id')->constrained('users');
            $table->foreignId('category_id')->constrained('categories');
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('events');
    }
};
